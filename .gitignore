# Repos secondary con historial y remote propios (no se anidan como submodule, se ignoran)
/weather-station-backend-service/
/weather-station-frontend-dashboard/
/weather-station-station-iot/

# OS / editor
.DS_Store
Thumbs.db
desktop.ini
.vscode/
*.swp

# Local env
.env
.env.local

# Banco de pruebas throwaway (Arduino Mega, no es parte del firmware del nodo)
/bench-veleta-anemometro/
